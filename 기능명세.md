# 하영의 지도 — 기능 명세서

> **한 줄 요약**
> 지금 이 서비스는 **"보는 것"은 거의 완성**, **"지우는 것"은 조건부 가능**, **"고치고 더하는 것"은 아직 안 됩니다.**
> 원래 목표였던 *네이버 즐겨찾기를 이 앱에서 열람·수정·삭제* 중에서 **열람 ✅ / 삭제 ✅ / 리스트 관리 ✅ / 수정 ❌ / 장소 추가 ❌** 상태입니다.
> (2026-08-20 갱신 — 3차 HAR로 **리스트 만들기·삭제**가 열렸습니다.)

- 주소: https://favplace-map.pages.dev (아이디 `favplace` + `SITE_PASS`)
- 기준일: 2026-08-19 / 배포 `bcb6d791` / 데이터 750곳

---

## 0. 먼저 알아야 할 것 — "스냅샷"과 "실시간"의 차이

이 앱에는 **두 종류의 데이터**가 있습니다. 헷갈리기 쉬워서 먼저 정리합니다.

| | 스냅샷 데이터 | 실시간 데이터 |
|---|---|---|
| 무엇 | 지도에 보이는 **750곳** | `네이버 원본 관리`에서 불러오는 목록 |
| 언제 만들어짐 | HAR을 캡처한 그 시점에 고정 | 버튼 누른 **바로 그 순간** |
| 담긴 정보 | 이름·좌표·사진·평점·메뉴·카테고리·지역 | 이름·폴더·메모 (**사진·평점 없음**) |
| 자동 갱신 | ❌ 안 됨 | 누를 때마다 새로 가져옴 |

**지도에 보이는 750곳은 네이버와 실시간으로 연결돼 있지 않습니다.** 그때 떠온 사본입니다.
네이버에서 뭘 추가/삭제했는지는 `네이버 원본 관리 → 지금 가져와서 비교하기`로 확인합니다.

---

## 1. 지금 바로 되는 것 ✅ (아무 설정 없이)

### 1-1. 열람 — 완성됨
| 기능 | 설명 |
|---|---|
| 지도 보기 | 750곳을 한국 지도에 표시. 가까운 핀은 **"동네 이름 + 개수"** 버블로 묶임 |
| 지역 필터 | 도 › 시군구 › 동 3단계 드릴다운 |
| 테마 필터 | 대분류 11종 › 세부 종류 (음식점 › 한식 등) |
| 검색 | 이름·지역·메뉴·설명 |
| 내 위치 | 현재 위치 표시 + 가까운 순 정렬 + 근처 추천 배너 |
| 장소 상세 | 사진 캐러셀·평점·리뷰수·한줄평·키워드·대표메뉴·전화·길찾기·공유 |
| 지도 새로고침 | 지금 보이는 지도 범위 안의 장소만 목록에 표시 |

### 1-2. AI 추천 코스 — 완성됨
자연어로 적으면 **저장한 장소들로** 코스를 짭니다. (예: "강릉에서 바다 보이는 카페랑 밥집")
결과는 지도 + 번호 경로 + 시간순 타임라인. 시간·이동수단·거리는 좌표로 계산됩니다. 엔진은 Gemini.

### 1-3. 내 기록 — 완성됨 (단, **네이버와 무관**)
방문 체크 / 나만의 메모 / 나만의 리스트 / 큐레이션 리스트 / 내보내기·가져오기.

> ⚠️ **중요**: 여기서 쓴 메모는 **이 기기 브라우저에만** 저장됩니다.
> 네이버 즐겨찾기의 메모와 **전혀 다른 것**이고, 서로 연동되지 않습니다.
> 폰을 바꾸거나 브라우저 데이터를 지우면 사라지므로 가끔 `내보내기`로 백업하세요.

### 1-4. 웹·태블릿 — 완성됨
768px 이상에서 좌측 패널 + 우측 지도 2단 레이아웃으로 자동 전환.

---

## 2. 쿠키만 등록하면 되는 것 🔓 (현재 잠김)

`내 기록` → 맨 아래 `네이버 즐겨찾기 가져오기 · 폴더 · 삭제`로 들어갑니다.
화면 맨 위에 **연결 상태**가 표시되므로 지금 켜져 있는지 거기서 바로 확인할 수 있습니다.

### 2-1. 최신 목록 가져와 비교하기
네이버 원본을 그 순간 불러와 앱 데이터와 대조합니다.
- 네이버 원본 N곳 vs 앱 데이터 N곳
- **앱에 없는 새 장소** 목록 (네이버에서 새로 저장한 것)
- **네이버에서 사라진 장소** 목록 (네이버에서 지운 것)

### 2-2. 리스트(폴더) 만들기 · 삭제하기 ✦ 신규
폴더 목록 화면에서:
- **`＋ 새 리스트 만들기`** — 이름을 입력하면 네이버에 **실제로 새 리스트가 생깁니다.**
- 각 리스트 오른쪽 **🗑** — 리스트를 삭제합니다.
  - **기본 폴더(내 장소)는 삭제 버튼이 없습니다.**
  - **장소가 든 리스트는 버튼이 비활성**입니다. 장소가 든 리스트를 지웠을 때 그 장소들이
    살아남는지 아직 검증되지 않아서 일부러 막아뒀습니다. 먼저 장소를 비운 뒤 삭제하세요.

### 2-3. 폴더에서 빼기 / 장소 삭제하기
폴더를 고르면 그 폴더의 장소가 체크박스 목록으로 나옵니다. 각 항목에 태그가 붙습니다.
- `폴더만` — 다른 폴더에도 있어서 **폴더에서만** 빠짐
- `삭제됨` — 이 폴더에만 있어서 빼면 **즐겨찾기에서 완전히 사라짐**

실행 전 **미리보기**가 반드시 뜹니다: "폴더에서만 제거 3곳 / ⚠️ 완전 삭제 2곳" + 이름 목록.
거기서 한 번 더 확인해야 실제로 실행됩니다. **한 번에 최대 25곳.** 되돌릴 수 없습니다.

### 2-4. 켜는 법 (하영이 직접 — 저는 쿠키 값을 보지 않습니다)
1. 크롬에서 map.naver.com 로그인 → `F12` → **Application** → Cookies → `NID_AUT`, `NID_SES` 값 복사
2. Cloudflare → Workers & Pages → **favplace-map** → Settings → Variables and secrets
3. `NAVER_COOKIE` = `NID_AUT=...; NID_SES=...` **(Secret으로)**
4. `NAVER_SYNC_WRITE` = `on`  ← 이게 없으면 읽기만 되고 삭제는 잠깁니다

> 쿠키는 **몇 주 뒤 만료**됩니다. 어느 날 갑자기 "미연결"로 뜨면 1~3번을 다시 하세요.
> 이게 이 방식의 가장 큰 불편입니다 (→ 5-2 참고).

---

## 3. 아직 안 되는 것 ❌

| 하고 싶었던 것 | 상태 | 왜 안 되는지 |
|---|---|---|
| **메모 수정** | ❌ | `save-widget` 북마크 PATCH가 필요. 캡처된 요청에 정체불명의 `token`이 있었음. **다만 토큰이 실은 선택값일 수 있어 시험 가치 있음** |
| **이름 변경** | ❌ | 위와 같은 API·같은 이유 |
| **다른 폴더로 옮기기/추가** | ❌ | 위와 같은 API·같은 이유 |
| **새 장소 추가** | ❌ | 장소를 **찾는** API는 확보했으나(`instant-search`), **저장하는** API는 미확보 |
| **리스트 만들기** | ✅ | 3차 HAR로 확보 (토큰 불필요) |
| **리스트 삭제** | △ | 빈 리스트만. 장소가 든 리스트는 동작 미검증이라 막아둠 |
| **리스트 이름 변경** | ❌ | 해당 API 미확보 |
| 가져오기가 지도까지 갱신 | ❌ | 사진·평점을 곳당 1.4초씩 긁어야 해서 브라우저에서 불가. PC에서 빌드 필요 |

### 왜 삭제는 되고 수정은 안 되나?
처음엔 "`save-widget` 계열은 전부 토큰이 필요하다"고 봤는데 **3차 HAR에서 뒤집혔습니다.**
리스트 만들기도 `save-widget`인데 **토큰이 없었습니다** (3차 HAR 전체에서 `"token"` 등장 **0회**).

→ 토큰이 필요한 건 아마 **북마크 PATCH 딱 하나**입니다.
→ 게다가 그것마저 **실은 선택값일 가능성**이 있습니다.
   **쿠키만 등록되면 토큰 없이 한 번 시험**해볼 수 있고, 통하면 수정 기능이 한 번에 열립니다.

---

## 4. 그래서 지금 어디까지 쓸 수 있나 (한눈에)

```
열람      ██████████ 100%   지도·검색·필터·상세·AI코스 전부 동작
                            (단 750곳은 스냅샷, 실시간 목록은 이름·폴더만)

삭제      █████████░  90%   장소 삭제 / 폴더에서 빼기 / 빈 리스트 삭제 (쿠키 등록 필요)
                            장소가 든 리스트 통째 삭제만 미검증이라 막아둠

리스트관리 ███████░░░  70%   만들기 ✅ / 삭제 ✅(빈 것만) / 이름 변경 ❌

수정      ░░░░░░░░░░   0%   메모·이름·폴더이동 전부 막힘 (token 미확보)

장소추가   ██░░░░░░░░  20%   검색 경로만 확보, 저장은 불가
```

---

## 5. 보완이 필요한 것 (우선순위 순)

### 🔴 1순위 — 북마크 PATCH를 토큰 없이 시도
3차 HAR에서 같은 `save-widget` 계열인 리스트 만들기가 **토큰 없이 동작**하는 걸 확인했습니다.
PATCH도 토큰이 실은 선택값일 가능성이 큽니다. **쿠키만 등록되면 제가 바로 시험해볼 수 있습니다.**
성공하면 **메모·이름변경·폴더이동이 한 번에 열립니다.** 실패하면 그때 아래 토큰 찾기로 넘어갑니다.

### 🔴 1순위-B — (위가 실패할 때만) `token` 출처 찾기

- **해야 할 일**: 저장 위젯이 **iframe 안**에 있어서 지난번 콘솔 검색이 빈손이었습니다.
  F12 콘솔 왼쪽 위 **`top ▾` 드롭다운을 `save-widget` 프레임으로 바꾼 뒤** 아래를 실행:
  ```
  copy(document.documentElement.innerHTML.match(/.{80}token.{120}/gi)?.join('\n---\n') ?? 'NONE')
  ```
- 안 나오면 대안: 메모를 수정하면서 **sanitized HAR**을 다시 캡처 (쿠키 없이도 됨)

### 🟠 2순위 — 쿠키 만료 대응
지금은 만료될 때마다 하영이 Cloudflare에 수동 재등록해야 합니다. 개선 방향:
- 앱에서 만료를 감지해 **"쿠키가 만료됐어요"** 라고 명확히 알려주기 (지금은 그냥 실패)
- 재등록 절차를 앱 안에서 단계별로 안내

### 🟡 3순위 — 데이터 갱신 반자동화
지금은 새 장소가 지도에 뜨려면 PC에서 `enrich.py` → `build_data.py` → 배포를 돌려야 합니다.
- GitHub Actions(`refresh.yml`)에 `NAVER_COOKIE`를 Secret으로 넣고 cron을 켜면 **매일 자동 갱신** 가능
- 이미 워크플로 파일은 만들어져 있고 스케줄만 주석 처리된 상태

### 🟡 4순위 — 장소 추가
검색 API는 확보했으니 UI는 만들 수 있지만, **저장 API가 없어 반쪽**이 됩니다.
저장 API를 찾기 전까지는 "검색해서 큐레이션 리스트에 담기"까지만 가능합니다.

### 🟢 5순위 — 그 외
- **동 정보 17곳 미수집** — 네이버 429(레이트리밋)로 중단됨. 쿨다운 후 `fill_dong.py` 재실행하면 끝
- **내 기록이 기기에만 저장됨** — 폰 바꾸면 사라짐. 서버 저장 or 자동 백업 검토
- **일괄 삭제 25곳 제한** — 안전장치로 건 값. 대량 정리하려면 여러 번 나눠야 함
- **되돌리기 없음** — 삭제 전 스냅샷을 저장해두면 "실수로 지운 것" 복구 안내 정도는 가능

---

## 6. 다음에 뭘 하면 가장 좋은가

| 순서 | 할 일 | 누가 | 걸리는 시간 | 열리는 것 |
|---|---|---|---|---|
| 1 | Cloudflare에 쿠키 2개 등록 | **하영** | 5분 | 가져오기·삭제·리스트 관리가 **실제로 동작** |
| 2 | PATCH를 토큰 없이 시험 | 나 | 즉시 | 통하면 **수정 기능 전체** |
| 3 | (2가 실패하면) `token` 찾기 | 하영 | 5분 | 메모·이름·폴더이동 |
| 4 | Actions 자동 갱신 켜기 | 하영+나 | 20분 | 매일 자동 데이터 갱신 |

**1번만 해도 "이 앱에서 내 네이버 즐겨찾기를 정리한다"가 실제로 됩니다.**
그리고 1번을 하면 2번은 제가 바로 이어서 확인할 수 있습니다.
