# 하영의 지도 (favplace) — 사용 매뉴얼

내가 네이버 지도에 저장한 장소 **726곳**을 한국 지도 위에서 사진·테마·지역으로 탐색하고,
방문 기록·나만의 리스트·AI 코스 추천까지 하는 **개인용 모바일 웹앱**.

- **주소**: https://favplace-map.pages.dev
- **로그인**: 접속하면 아이디 `favplace` + 비밀번호(내가 Cloudflare에 설정한 `SITE_PASS`) 입력
- **앱처럼 쓰기**: 폰 브라우저에서 열고 **`홈 화면에 추가`** → 주소창 없는 전체화면(PWA)으로 실행
- **데이터 기준일**: 최근 빌드 기준 726곳 / 사진 595곳 / 폐업 5곳 / 동 정보 684곳(94%)

---

## 1. 화면 한눈에 보기

| 위치 | 요소 | 설명 |
|---|---|---|
| 지도 전체 | **핀 / 클러스터** | 핀 색 = 테마(카테고리). 숫자 원(클러스터)을 탭하면 펼쳐짐 |
| 상단 | **검색 필드**(어디로 가볼까요) + **하영** 프로필 버튼(가이드) | 시안 1b |
| 지도 우측 | ◎ **내 위치** | 시트가 내려가 있을 때만 표시 |
| 상단 칩 줄 | **지역 · 테마 · 내 기록** | 필터 칩 3개. 각각 누르면 드릴다운 시트가 열림 |
| 우하단 | ✨ **추천 코스** (FAB) | AI 코스 추천 열기 |
| 하단 | **바텀시트** | 장소 목록 / 상세가 뜨는 시트 (위로 끌어올리기) |
| 하단 중앙 | **`▲ 목록 N곳`** | 시트를 완전히 내렸을 때만 나타나는 탭 버튼 |

---

## 2. 지도 보기

- **핀 색이 곧 테마**입니다. (음식점=코랄, 카페=캐러멜, 술집·바=페리윙클, 문화=민트, 레저=청록, 명소=핑크, 자연=연두, 숙박=블루, 쇼핑=오키드, 주거·부동산=슬레이트, 생활=그레이)
- 앱 키컬러는 **하늘색**입니다. 선택된 필터·버튼은 하늘색 그라디언트로 표시돼요.
- 가까이 있는 핀은 **클러스터(숫자 원)** 로 묶입니다. 탭하면 확대되며 펼쳐져요.
- 핀을 탭하면 그 장소의 **상세 시트**가 열립니다.
- 필터(지역·테마·검색·내 기록)를 걸면 지도의 핀도 함께 걸러집니다.

---

## 3. 지역 필터 — 도 › 시군구 › 동

상단 첫 칩 **`지역`** → 단계별로 좁혀 들어갑니다.

1. **도/시** 선택 (예: 경기 370, 서울 188, 대전 64 …)
2. **시군구** 선택 (예: 용인시 처인구 131, 성남시 분당구 42 …)
3. **동/읍면리** 선택 (예: 정자동 9, 양지면 20 …)

- 각 항목 옆 숫자 = 그 지역의 저장 장소 수.
- 상단 **breadcrumb(전국 › 경기 › 성남시 분당구)** 를 탭해 상위로 되돌아갈 수 있어요.
- 각 단계에 **`○○ 전체`** 가 있어 그 레벨 전체로 필터할 수 있습니다.
- 동 정보가 아직 없는 곳은 **"그 외 N곳은 동 정보가 아직 없어요"** 로 안내됩니다(전체 94% 확보).
- 지역 칩을 적용하면 지도가 그 지역으로 **자동 확대**됩니다. 칩의 **✕** 로 초기화.

---

## 4. 테마(카테고리) 필터 — 대분류 › 세부

상단 **`테마`** 칩 → 지역과 똑같은 드릴다운 시트가 열립니다.

1. **대분류** 선택 (음식점 258 · 카페 195 · 주거·부동산 87 · 생활 71 · 문화 34 …) — 누르면 바로 적용
2. 오른쪽 **`›`** 를 누르면 **세부 종류**로 좁혀 들어갑니다
   - 예) **음식점** → 한식 93 · 고기·구이 51 · 양식·아시안 41 · 일식·회 32 · 중식 19 · 분식 15 · 치킨 7
   - 예) **카페** → 디저트 80 · 커피 46 · 베이커리·빵 27 · 브런치 12 · 베이글 6 …
3. 선택하면 칩이 **`한식 ×`** 처럼 바뀌고, **✕** 로 해제합니다.
- 분류는 네이버 카테고리 + **장소명·설명**을 함께 분석해 자동 분류됩니다.
- 지역·검색·내 기록 필터와 **자유롭게 조합**됩니다. (예: 서울 × 카페 = 41곳)

---

## 5. 검색

- 상단 **검색 필드** 탭 → **이름·지역·메뉴·설명**으로 검색. 검색 중이면 필드에 검색어가 표시됩니다.
- 입력 즉시 지도·목록이 걸러집니다. ✕ 로 닫기.

---

## 6. 내 위치

- 지도 우측 ◎ 버튼 → 위치 권한 허용 시 **현재 위치에 파란 마커(펄스)** 표시 + 지도 이동.
- 이후 목록이 **가까운 순 / 추천 순** 토글로 정렬됩니다.
- 근처에 저장 장소가 있으면 상단에 **근처 추천 배너**가 뜹니다.

---

## 7. 내 기록 — 방문·메모·리스트 ⭐ (이 앱만의 기능)

상단 **`내 기록`** 칩 → 나만의 데이터를 관리합니다. **이 기기(브라우저)에 저장**돼요.

### 7-1. 빠른 필터
- **전체 보기 / ✓ 가본 곳 / 아직 안 간 곳** 으로 한 번에 필터.

### 7-2. 방문 체크 (가봤어요)
- 장소 상세에서 **`가봤어요`** 를 누르면 체크(초록). 목록/지도에서 방문한 곳은 이름 앞에 ✓ 표시 + 살짝 흐리게.

### 7-3. 나만의 메모
- 장소 상세의 **`내 메모`** 칸에 자유롭게 적으면 자동 저장. (네이버에 적어둔 메모는 "네이버 메모"로 따로 보여요)

### 7-4. 나만의 리스트 (내가 직접 담기)
- 상세의 **`☆ 리스트에 추가`** → 기존 리스트에 담거나 **새 리스트**를 즉석에서 만들어 담기.
- `내 기록` 화면의 **`내 리스트`** 에서 리스트별로 필터 / 삭제 / 새로 만들기.

### 7-5. 큐레이션 리스트 (내가 정보 주면 채워지는 리스트) ✦
- `내 기록` 화면 상단 **`큐레이션 리스트`** 섹션.
- 지금 만들어 둔 예시: **경기남부 가성비 골프장(3)**, **서울·경기 음악감상실(1)**.
- **채우는 법**: 카카오톡·메모 등으로 **장소 이름(가능하면 주소/지역)을 쭉 보내주면**, 제가 그 리스트에 넣어드립니다.
  - 저장 장소면 그대로 연결, **저장 안 된 장소도** 위치를 찾아 리스트에 추가해 지도에 표시합니다.
  - "새 리스트 이름 = ○○, 장소 = A, B, C…" 형식이면 가장 빠르게 반영돼요.

### 7-6. 기록 내보내기 / 가져오기
- `내 기록` 화면 맨 아래 **`기록 내보내기`** → 방문·메모·리스트를 JSON 파일로 백업.
- **`가져오기`** → 백업 파일을 불러와 합치기(다른 기기로 옮길 때 사용).
- ⚠️ 방문·메모·나만의 리스트는 **기기별 저장**이라, 폰을 바꾸거나 브라우저 데이터를 지우면 사라질 수 있어요. 가끔 내보내기로 백업 권장.

---

## 8. 장소 상세

핀이나 목록 카드를 탭하면 시트가 **전체로 확장**되며 상세가 열립니다.

- **사진 캐러셀**(좌우 스와이프, 하단 점 인디케이터), 이름·테마·평점·리뷰수·주소·폴더
- **한 줄 리뷰 요약**, **#키워드**(룸·주차·오션뷰·데이트 등), **대표 메뉴/가격**
- **가봤어요 · 리스트에 추가 · 내 메모** (7번 참고)
- 하단 액션: **네이버**(네이버 지도 페이지) · **길찾기**(네이버 지도 앱 연결) · **전화** · **공유**
- **✨ 이런 곳 더 추천받기** → 이 장소와 비슷한 분위기로 AI 코스 생성

---

## 9. AI 추천 코스 (리포트형)

우하단 **✨ 추천 코스** → 기분·상황을 자연어로 적으면 **저장 장소들로** 코스를 짜줍니다.

- 예) "노을 볼 수 있는 시원한 실내 공간", "용인에서 분위기 좋은 카페랑 밥집 코스", "제주 빵지순례"
- 결과는 **리포트 형태**로 나옵니다:
  - **코스 제목** + **`AI 분석` 노트**(왜 이렇게 짰는지 큐레이션 설명)
  - **1→2→3→4 순서 코스** (각 장소별 추천 이유/팁)
  - **실용 팁**(동선·시간·주차 등) + 지도에 **동선 라인** 표시
- 엔진: 구글 **Gemini**(`gemini-2.0-flash`). 키/네트워크 문제 시 자동으로 **간이 추천**으로 폴백.

---

## 10. 바텀시트 조작

- 하단 시트는 **peek(120px) → 기본(452px) → 확장(상단 200px 남김)** 3단계 + **완전히 숨김**.
- **손잡이(막대)를 탭**하면 한 단계씩 올라가고, **위/아래로 끌면** 실시간으로 따라오며 스냅됩니다.
- 손잡이 오른쪽 **`∨`** 를 누르면 시트가 **화면 밖으로 완전히 사라지고**, 하단 중앙에 **`▲ 목록 N곳`** 탭 버튼만 남습니다. → 지도를 넓게 볼 때 사용. 탭 버튼을 누르면 다시 올라와요.
- peek 상태에서 **아래로 쓸어내려도** 완전히 숨겨집니다.
- 상세를 보다가 **아래로 쓸어내리면** 접혀요(내용 맨 위에서 당기면 닫힘).

---

## 11. 가이드 & 현황

- 상단 우측 **하영** 버튼 → 전체/사진/폐업/갱신일 **통계**, 테마별 개수, 기능 요약을 한 곳에서 봅니다.

---

## 12. 데이터 & 자동 갱신

- 원본: 네이버 지도 **즐겨찾기 726곳**. 중복 제거·폐업 표시(5곳)·좌표/사진/평점/메뉴 보강 완료.
- **동 정보**: 네이버 지번주소에서 추출해 150 → **684곳(94%)** 확보(`fill_dong.py` → `dong_map.json`).
- **자동 갱신(선택)**: GitHub Actions(`.github/workflows/refresh.yml`).
  현재는 네이버 세션 쿠키(`NAVER_COOKIE`)가 없어 **스케줄 비활성**(수동 실행만). 쿠키를 GitHub Secret으로 넣고 cron 주석을 풀면 매일 자동 갱신됩니다.

---

## 13. 운영 / 배포 (관리자용)

- 호스팅: **Cloudflare Pages** 프로젝트 `favplace-map` (개인 계정, accountId `7f53aeca…`).
- 배포: `app/` 안에서
  ```bash
  export CLOUDFLARE_ACCOUNT_ID=7f53aeca9212909eb3f3f0d62b814c20
  npx wrangler pages deploy . --project-name favplace-map --branch main --commit-dirty=true
  ```
  (배포 직후 node가 크래시해도 배포는 성공. 안 되면 `.wrangler` 캐시 삭제 후 재시도)
- 환경변수(Cloudflare 대시보드 → 프로젝트 → Variables and secrets):
  - **`SITE_PASS`**: 접속 비밀번호(엣지 Basic Auth). 미설정 시 사이트 전체 503으로 잠김.
  - **`GEMINI_KEY`**: AI 추천용 Gemini API 키.
- 데이터 다시 빌드: `python build_data.py` → `app/data/places.json`·`meta.json` 생성.
- 캐시: 정적 파일은 `?v=` 버전으로 갱신(현재 **v33**). 문서(index.html)는 네트워크 우선이라 새로고침이면 최신.
  ⚠️ css/js를 고치면 `index.html`의 `?v=` 숫자를 반드시 올려야 서비스워커 캐시가 갱신됩니다.
- 로컬에서 열어보기: `.claude/launch.json`의 `app` 구성(파이썬 정적 서버, 포트 8765).
  단 정적 서버라 **AI 추천·엣지 인증은 동작하지 않습니다**(Pages Functions 미실행). 디자인/UI 확인용.

---

## 14. 향후: 네이버 원본 동기화(삭제·폴더·메모)

favplace에서 바꾼 걸 **네이버 즐겨찾기 원본에도 반영**하는 기능은, 되돌리기 어려운 삭제/수정을 무인 실행하지 않기 위해 **끄고** 두었습니다. 켤 수 있게 스캐폴드는 만들어 두었어요: `app/functions/naver-sync.js`.

**이중 안전장치** (스캐폴드에 이미 반영):
1. `NAVER_COOKIE` 환경변수가 없으면 아무것도 안 함(503).
2. 쓰기(삭제/메모/폴더)는 `NAVER_SYNC_WRITE=on` 일 때만 허용.
3. 아직 네이버 "쓰기 API 형식"을 채우지 않아, 쓰기는 501(미구현)로 막힘 → 실제로는 아무 것도 지우지 않음.

**켜려면 하영이 제게 줄 정보 2가지:**
- **A. 쓰기 API 캡처** — 네이버 지도(PC 크롬)에서 즐겨찾기 **삭제 / 메모수정 / 폴더이동**을 실제로 한 번씩 하면서, **개발자도구(F12) → Network** 탭에서 그 요청을 찾아 아래를 복사해 전달:
  - `Request Method`(POST/DELETE 등), `Request URL`
  - `Request Headers` (단, **Cookie 값은 지우고**/가리고 주세요 — 헤더 "구조"만 필요)
  - `Request Payload`(요청 본문/JSON)와 응답 예시
  → 이걸 주시면 제가 `naver-sync.js`의 TODO 부분을 그 형식대로 구현합니다.
- **B. 네이버 세션 쿠키** — 이건 **비밀값이라 제가 아니라 하영이 직접** Cloudflare에 등록합니다(저는 값을 보지 않아요).
  - 크롬에서 map.naver.com 로그인 → 개발자도구 → Application → Cookies → `NID_AUT`, `NID_SES` 값 복사
  - Cloudflare 대시보드 → favplace-map → Settings → Variables and secrets →
    `NAVER_COOKIE` = `NID_AUT=...; NID_SES=...` (Secret), `NAVER_SYNC_WRITE` = `on`

그전까지는 **내 기록(방문·메모·리스트)** 로 favplace 안에서 관리하고 **내보내기**로 백업하세요.

---

## 15. 보안 / 프라이버시

- 집 주소·주거지 등 개인 장소가 포함되므로 사이트는 **비밀번호(엣지 인증)** 로 보호됩니다.
- 방문·메모·리스트는 **서버가 아니라 내 기기(브라우저)** 에만 저장됩니다.
- 큐레이션 리스트(`lists.json`)는 배포 파일이라, 민감한 개인 메모는 넣지 않습니다.

---

## 16. 웹(데스크톱)에서 보기 — 반응형

- **768px 미만(폰)**: 지금까지의 지도 + 바텀시트 레이아웃 그대로.
- **768px 이상(태블릿·PC)**: 바텀시트가 **좌측 고정 패널**(360px, 1200px 이상에선 420px)로 바뀌고
  지도가 나머지를 채웁니다. 검색·필터 칩도 패널 안으로 들어갑니다.
  - 지역·테마·내 기록·가이드 오버레이는 **가운데 모달 카드**로 뜹니다.
  - AI 코스 결과는 **좌 지도 / 우 코스** 2단이 됩니다.
  - 마우스 환경에서만 hover 효과가 붙고, 키보드 포커스 링(`:focus-visible`)이 표시됩니다.
- 1800px가 넘어가면 앱 폭이 1800px로 제한되고 가운데 정렬됩니다.

## 17. 전체 화면 목업 보기 (관리자용)

`/mockups.html` 에 **모든 화면을 한 페이지에 모아둔 살아있는 목업**이 있습니다.
스크린샷이 아니라 실제 앱을 iframe으로 띄운 것이라 지도도 움직이고 버튼도 눌립니다.

- 폰 3종(시트 peek/기본/확장), 장소 상세, 검색, 지역/테마/내 기록/가이드,
  AI 코스 입력·결과, 데스크톱 1180px·태블릿 834px, 그리고 **원본 승인 시안**까지 한 눈에.
- 개별 화면은 `index.html?screen=…` 로 직접 열 수 있습니다:
  `home · list · list-full · detail · region · theme · mine · guide · search · course · course-result`
- 원본 시안 파일은 저장소 `design/` 에 있습니다(`지도 홈 시안.dc.html`, `support.js`, `README.md`).
  `app/design-ref.html`·`app/support.js` 는 배포본에서 목업 페이지가 시안을 띄우기 위한 사본입니다.

## 18. 네이버 원본 관리 (가져오기 · 폴더 · 삭제)

`내 기록` 화면 맨 아래 **`네이버 즐겨찾기 가져오기 · 폴더 · 삭제`** 로 들어갑니다.

### 18-1. 지금 가져와서 비교하기
네이버에 저장된 **지금 이 순간의 원본**을 불러와 앱 데이터와 대조합니다.
- 네이버 원본 N곳 / 앱 데이터 N곳 / **앱에 없는 새 장소** / **네이버에서 사라진 장소** 를 목록으로 보여줍니다.
- ⚠️ 지도에 실제로 반영하려면 사진·평점 보강이 필요해서 PC에서 한 번 빌드해야 합니다
  (`python enrich.py` → `python build_data.py` → 배포). 앱에서는 **무엇이 달라졌는지**까지 확인합니다.

### 18-2. 폴더 관리 · 삭제
폴더를 고르면 그 폴더의 장소가 체크박스 목록으로 나옵니다.
- 각 장소에 **`폴더만`**(다른 폴더에도 있음) 또는 **`삭제됨`**(이 폴더에만 있어서 빼면 즐겨찾기에서 사라짐) 태그가 붙습니다.
- 선택 후 실행하면 **먼저 미리보기**가 뜹니다: 폴더에서만 빠지는 곳 N개, 완전 삭제되는 곳 N개를 이름까지 보여주고
  거기서 한 번 더 확인해야 실제로 실행됩니다. 한 번에 최대 25곳.
- 되돌릴 수 없습니다.

### 18-3. 켜는 법 (하영이 직접)
쿠키는 비밀값이라 앱·저장소에 넣지 않습니다. Cloudflare에 직접 등록해야 합니다.
1. 크롬에서 map.naver.com 로그인 → F12 → Application → Cookies → `NID_AUT`, `NID_SES` 복사
2. Cloudflare → Workers & Pages → favplace-map → Settings → Variables and secrets
3. `NAVER_COOKIE` = `NID_AUT=...; NID_SES=...` (Secret)
4. 수정·삭제까지 쓰려면 `NAVER_SYNC_WRITE` = `on`

등록 전에는 화면에 이 안내만 뜨고 **아무것도 실행되지 않습니다**. 쿠키는 몇 주 뒤 만료되니 안 되면 다시 넣으세요.

### 18-4. 아직 안 되는 것
**메모 수정 · 이름 변경 · 다른 폴더로 추가**는 네이버의 다른 API(`save-widget` PATCH)가 필요한데
그 요청에 들어가는 `token` 의 출처를 아직 못 찾아서 막혀 있습니다(501).
